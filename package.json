{
	"scripts":{
		"start":"elm-live --dir=docs --output=docs/index.js --debug src/Main.elm",
		"build":"elm make --optimize --output=docs/index.js src/Main.elm"
	},
  "devDependencies": {
    "elm-live": "^3.0.1"
  }
}
